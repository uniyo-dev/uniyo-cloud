services:
  - type: web
    name: uniyo-cloud
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cloud_server:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
